// para instalar os modulos js
// npm install init = isso cria o arquivo package.json
/* npm i --save express@4.16.2 -E = instalando o frame express na versão(@)4.16.2  (-E) quer dizer que será essa versão exata.
Isso é bom para manter padrões de projeto e não ter problemas com mudanças/atualizações.*/
// npm i --save nodemon = instalar o nodemon 
// npm i --save body-parser = fazer uma conversão no body da requisição, construindo um valor ou um obj.
// npm i node-schedule = um pacote de node que possibilita agendar tarefas para o sistema.
// npm i -g http-server =  para inicializar o servidor na pagina web atraves de uma porta. O mesmo que o go life faz.
// http-server -c10 = guarda os arquivos no cash por 10 segundos
// http-server -c-1 . = não quer que seja armazenado os arquivos em cash. (.)para ele continuar servindo a mesma pasta de antes